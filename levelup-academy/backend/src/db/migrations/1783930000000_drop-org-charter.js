/**
 * Свободный текстовый устав организации убран 2026-07-28 — заменён каталогом
 * структурированных правил (discipline_rules, см. 1783920000000). Karis: одних
 * правил достаточно, вести параллельно ещё и прозу не нужно.
 */
export const up = (pgm) => {
  pgm.sql(`DROP TABLE IF EXISTS org_charters;`);
};

export const down = () => {
  // Проект мигрирует только вперёд (см. CONSTRAINTS.md) — таблицу и её данные
  // назад не восстанавливаем, down() здесь для симметрии.
};
