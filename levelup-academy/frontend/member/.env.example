# ── LOCAL DEV ──
# По умолчанию фронт ходит на БОЕВОЙ бэкенд (Render) — НИЧЕГО настраивать не нужно.
# Просто: npm install && npm run dev  →  вход и данные работают.
#
# Только если ты BACKEND-разработчик и поднял бэк локально на :4000 — раскомментируй:
# DEV_API_PROXY=http://localhost:4000
