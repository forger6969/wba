# WBA — рабочий план (2026-09-19)

Живой сайт: **https://wba-peach.vercel.app** · Вход: `jamshid` / (админ-пароль).
База: Supabase `uxmmyrfzcccieuqaosts`. Репозиторий: `jamshideng/wba` (main).

---

## ✅ Уже сделано
- Supabase-облако: миграции 0001–0017, данные из Sheets (79 учеников, 21 группа, 40 оплат, 268 уроков).
- Аккаунты: админ + 9 учителей (логин=имя) + 79 учеников (логин=5-значный). Файлы в `.secrets/`.
- Код-фиксы: дыра anon-EXECUTE, редирект-цикл /crm, open-redirect, гард давомата, пагинация, rate-limit, error/not-found/robots/sitemap.
- Тёмная/светлая тема: переключатель, no-FOUC, токены.
- Деплой разблокирован (коммит от члена команды), сайт живой.

---

## 🔨 Делаем (мой фронт)

### T1 — i18n публичного сайта (UZ + RU)
- [ ] Инфраструктура: cookie `wba-til`, словарь `src/lib/i18n.ts`, хелпер `t()`.
- [ ] Переключатель языка (UZ/RU) в шапке сайта.
- [ ] Перевести: шапка/меню, hero, секции главной, footer, `/ariza`, `/kirish`.
- [ ] `<html lang>` и `alternate` в метаданных по языку.
- CRM остаётся на узбекском (внутренний инструмент).

### T2 — Светлая тема: вычитка всех экранов
- [ ] Пройти CRM + сайт в light-режиме, убрать хардкод-цвета, проверить контраст (AA).
- [ ] Проверить формы, бейджи, графики (BarRow), карточки.

### T3 — SEO и мелочи
- [ ] og-image (динамический или статичный), favicon.
- [ ] Метаданные страниц (title/description) на обоих языках.
- [ ] Проверка Lighthouse (перф/доступность).

---

## 🙋 На тебе (я технически не могу)
- [ ] Разослать логины/пароли (файлы `.secrets/ustozlar-hisob.xlsx`, `oquvchilar-hisob.xlsx`) — защита среды не даёт мне слать секреты наружу.
- [ ] Vercel → Settings → Deployment Protection → **выключить SSO** (иначе preview-ссылки за логин-стеной).
- [ ] Отозвать Supabase Management-токен (`sbp_…`) на supabase.com/dashboard/account/tokens.
- [ ] (Опц.) Vercel ↔ GitHub интеграция, чтобы `push` в main = авто-деплой.
- [ ] (Опц.) Домены `wba.uz` / `app.wba.uz` — привязка + DNS.

---

## 🕐 Позже / друг
- [ ] Telegram-бот @WBAlcBot — по спеке `docs/BOT.md` (пишет друг).
- [ ] Двусторонняя синхронизация Sheets ⇄ БД (Faza 3).
